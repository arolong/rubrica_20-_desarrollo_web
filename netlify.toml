[build]
  command = "npx prisma generate --schema=prisma/schema.postgres.prisma && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Increase default timeout for serverless functions if needed
  node_bundler = "esbuild"
